# Стандарты проекта "Книги Ивана"

## 📁 Структура файлов

```
ai-books/
├── index.html              # Веб-viewer
├── data/
│   └── content.json        # Контент всех книг (тексты + пути к картинкам)
├── Главы/                  # Папка с главами
│   ├── Глава 01/
│   │   ├── синопсис.md     # План главы
│   │   ├── Страница 01/
│   │   │   ├── сценарий.md # Текст + промпт
│   │   │   └── render.png  # Изображение (ОБЯЗАТЕЛЬНО render.png!)
│   │   └── ...
│   └── Глава 02/
└── Мир и Лор/              # Лор, персонажи, стиль
```

## 📝 Именование файлов

### ОБЯЗАТЕЛЬНО:
| Файл | Название | Язык |
|------|----------|------|
| Изображение страницы | `render.png` | Английский |
| Сценарий | `сценарий.md` | Русский |
| Синопсис главы | `синопсис.md` | Русский |

### ❌ НЕ использовать:
- `рендер.png` — только `render.png`
- `render.jpg` — только `.png`
- Другие названия для изображений

## 📄 Формат сценарий.md

```markdown
# Сценарий Страницы XX

## Текст (Левая страница)

[Текст страницы на русском. Каждый абзац через пустую строку.]

## Иллюстрация (Правая страница)

**Описание сцены:**
[Краткое описание что изображено]

**Промпт для генерации:**
```
[Промпт на английском по стандарту PROMPTING.md]
```
```

## 🖼️ После генерации изображения

1. Сохранить как `render.png` в папку страницы
2. Обновить `data/content.json`:
   - Добавить/проверить текст страницы
   - Указать путь к изображению: `"image": "Главы/Глава XX/Страница XX/render.png"`
3. Закоммитить изменения

## 🔄 Workflow создания страницы

1. **Идея** → обсуждение сцены
2. **Сценарий** → создать `сценарий.md` с текстом и промптом
3. **Генерация** → ChatGPT/DALL-E → получить изображение
4. **Сохранение** → `render.png` в папку страницы
5. **content.json** → обновить данные
6. **Коммит** → push в репо
7. **Проверка** → открыть https://mikenesterov.github.io/ai-books/
